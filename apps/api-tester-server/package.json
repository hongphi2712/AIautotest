{
  "name": "api-tester-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "predev": "node scripts/cleanup-dev.js",
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:release": "taskkill /F /IM api-tester-server.exe /T >nul 2>&1 & cargo build --release -p api-tester-server && start \"\" ..\\..\\target\\release\\api-tester-server.exe",
    "clean:target": "powershell -NoProfile -ExecutionPolicy Bypass -File ../../scripts/gc-target.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
